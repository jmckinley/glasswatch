# Core dependencies for Glasswatch backend

# FastAPI and async server
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Database
sqlalchemy==2.0.31
asyncpg==0.29.0
alembic==1.13.2
psycopg2-binary==2.9.9  # For Alembic

# Configuration and environment
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.3.4

# API and security
httpx==0.27.0  # For async HTTP requests
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.0.1  # Direct bcrypt (passlib 1.7.4 + bcrypt 4.x compat)
workos==4.6.0  # WorkOS SSO integration

# Redis for caching and rate limiting
redis[hiredis]==5.0.7

# Date and time
python-dateutil==2.9.0

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# Development tools
black==24.4.2
isort==5.13.2
flake8==7.1.0
mypy==1.10.1

# Monitoring and logging
structlog==24.2.0

# Background tasks and scheduling
apscheduler==3.10.4  # For auto-sync scheduler
# temporal-sdk==1.6.0  # Future: more robust workflow engine

# Cloud provider SDKs (optional, for connection health checks)
boto3==1.34.162  # AWS SDK

# Graph database client (optional for now)
# neo4j==5.21.0
# gremlinpython==3.7.1  # For Memgraphsentry-sdk[fastapi]==2.5.1
