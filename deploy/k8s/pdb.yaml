apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: glasswatch-backend-pdb
  namespace: glasswatch
spec:
  minAvailable: 2  # Always keep at least 2 pods running
  selector:
    matchLabels:
      app: glasswatch
      component: backend
